The goal of this basic restaurant page was to practice the use of ES6 Modules and Webpack.
Instead of hardcoding each tab's content into the HTML, the content gets generated dynamically by each module when the corresponding tab is clicked.
